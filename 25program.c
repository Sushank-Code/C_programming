#include <stdio.h>

int main()
{
    int a = 0;
    // scanf("%d",&a);
    while (a <= 20)
    {
        if (a >= 10)
            printf("the natural number from 10 t0 20 is %d\n",a);
        a++;
    }
    return 0;
}
// WAp to print natural number from 10 t0 20